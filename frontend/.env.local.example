# Copy to .env.local and adjust as needed. Frontend env vars are prefixed
# NEXT_PUBLIC_ so they are inlined into the client bundle at build time.

# Base URL of the FastAPI backend. Used to build API request URLs.
NEXT_PUBLIC_API_URL=https://the-interview-agent-dxin.onrender.com
